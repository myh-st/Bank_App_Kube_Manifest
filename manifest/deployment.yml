
apiVersion: apps/v1
kind: Deployment
metadata:
  name: Bank-App
spec:
  selector:
    matchLabels:
      app: Bank-App
  replicas: 2
  template:
    metadata:
      labels:
        app: Bank-App
    spec:
      containers:
      - name: hmsapp
        image: savytech/bank-app:build-19
        ports:
        - containerPort: 80
